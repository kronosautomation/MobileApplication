// Custom App Entry point that explicitly imports from the correct location
import { registerRootComponent } from 'expo';
import App from './App';

// Explicitly register the App component
registerRootComponent(App);
